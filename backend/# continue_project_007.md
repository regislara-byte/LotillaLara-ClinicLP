# CONTINUE PROJECT

This is the current production Google Apps Script backend (e009).

It has been copied directly from Google Apps Script.

Read it before making changes.

Do NOT rewrite it.

Apply surgical patches only.

Preserve all existing functions.

Add only the missing backend actions:

- getAppointments()
- getSettings()
- updateSettings()
- rescheduleAppointment()

Reuse existing helper methods.

Reuse existing architecture.

No duplicated logic.

Update documentation.

Proceed.